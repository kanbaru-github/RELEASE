<% if post.sympathied_by?(current_customer) %>
  <%= link_to post.sympathies.count, post_sympathies_path(post), method: :delete, remote: true %>
<% else %>
  <%= link_to post.sympathies.count, post_sympathies_path(post), method: :post, remote: true %>
<% end %>